# Workout Shuffle

start with `python3 -m http.server 8000`


# Context
You are designing a workout app that allows users to both save their workout plans and also create workout templates which will then shuffle the exercises based on the templates configuration.
